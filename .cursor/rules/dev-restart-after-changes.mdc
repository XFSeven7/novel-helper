---
description: 改完 server/ui 后自动重启本地 dev，便于用户直接验证
alwaysApply: true
---

# 完成后自动重启 dev

在本仓库完成**会影响运行时代码**的修改后（尤其是 `packages/server`、`packages/ui`），在回复用户「已完成」之前，**必须**执行重启，无需用户再提醒：

1. 若刚执行过 `pnpm build` 或改动了 `packages/server/src/**`，优先重启后端（或整栈）。
2. 在项目根目录执行：

```bash
pnpm dev:restart
```

3. 等待日志出现 `Server listening at http://127.0.0.1:3177`（及 Vite 就绪）后再结束任务。
4. 在回复中简短说明已重启（端口：API `3177`，前端以 Vite 输出为准，常见 `5173`/`5178`）。

**例外**（可不重启）：仅改文档、`*.md`、`.gitignore`、纯前端样式且 HMR 已足够、或用户明确说不要重启。

**不要**在未完成验证命令的情况下声称已重启。
